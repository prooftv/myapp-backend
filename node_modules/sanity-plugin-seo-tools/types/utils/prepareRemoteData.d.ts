import type { PreparedData } from '../context';
declare type Options = {
    contentSelector?: string;
};
export declare function prepareRemoteData(url: URL, options: Options): Promise<PreparedData>;
export {};
